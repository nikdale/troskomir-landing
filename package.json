{
  "name": "troskomir-landing",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/apply-csp-hashes.mjs",
    "check": "astro check",
    "validate": "npm run check && npm run build && npm test",
    "validate:release": "node scripts/validate-release-config.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "test": "node --test tests/*.test.mjs",
    "check:delete-account": "node scripts/check-delete-account-path.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.2.8",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "js-yaml": "4.3.2",
    "fast-uri": "3.1.7",
    "sharp": "0.35.4",
    "svgo": "4.1.0"
  }
}
